import { XzcDirective } from './xzc.directive';

describe('XzcDirective', () => {
  it('should create an instance', () => {
    const directive = new XzcDirective();
    expect(directive).toBeTruthy();
  });
});
