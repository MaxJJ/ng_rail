import { RtmeH1Directive } from './rtme-h1.directive';

describe('RtmeH1Directive', () => {
  it('should create an instance', () => {
    const directive = new RtmeH1Directive();
    expect(directive).toBeTruthy();
  });
});
