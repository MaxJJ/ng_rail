import { FixedBlockDirective } from './fixed-block.directive';

describe('FixedBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new FixedBlockDirective();
    expect(directive).toBeTruthy();
  });
});
