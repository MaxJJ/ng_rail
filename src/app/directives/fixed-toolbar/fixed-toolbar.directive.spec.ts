import { FixedToolbarDirective } from './fixed-toolbar.directive';

describe('FixedToolbarDirective', () => {
  it('should create an instance', () => {
    const directive = new FixedToolbarDirective();
    expect(directive).toBeTruthy();
  });
});
