import { TopRightDirective } from './top-right.directive';

describe('TopRightDirective', () => {
  it('should create an instance', () => {
    const directive = new TopRightDirective();
    expect(directive).toBeTruthy();
  });
});
